services:
  backend-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-app-be-dev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./storage/uploads:/app/storage/uploads
